<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1.0' />
    <title>{{appName}} — Email verification</title>
    <style>
      /* General resets */
      body {
        margin: 0;
        padding: 0;
        background-color: #f4f6f8;
        font-family:
          -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          'Helvetica Neue', Arial;
      }
      .container {
        width: 100%;
        max-width: 600px;
        margin: 0 auto;
      }
      .card {
        background: #ffffff;
        border-radius: 8px;
        padding: 24px;
        box-shadow: 0 2px 6px rgba(16, 24, 40, 0.06);
      }
      .preheader {
        display: none !important;
        visibility: hidden;
        opacity: 0;
        color: transparent;
        height: 0;
        width: 0;
      }
      h1 {
        font-size: 20px;
        margin: 0 0 8px;
        color: #0f172a;
      }
      p {
        color: #334155;
        line-height: 1.5;
        font-size: 15px;
        margin: 0 0 16px;
      }
      .code-box {
        background: #f1f5f9;
        border-radius: 6px;
        padding: 14px 18px;
        font-size: 22px;
        letter-spacing: 4px;
        font-weight: 600;
        text-align: center;
        color: #0b1220;
        display: inline-block;
      }
      .btn {
        display: inline-block;
        padding: 12px 20px;
        border-radius: 6px;
        background: #0b74ff;
        color: #ffffff;
        text-decoration: none;
        font-weight: 600;
      }
      .muted {
        color: #64748b;
        font-size: 13px;
      }
      .footer {
        color: #94a3b8;
        font-size: 13px;
        text-align: center;
        padding: 18px 0 30px;
      }
      @media (max-width: 480px) {
        .card {
          padding: 18px;
        }
        .code-box {
          font-size: 20px;
          letter-spacing: 3px;
        }
      }
    </style>
  </head>
  <body>
    <!-- Preheader text for inbox preview -->
    <div class='preheader'>Your
      {{appName}}
      verification code:
      {{verificationCode}}
      (expires in
      {{expirationMinutes}}
      minutes)</div>

    <table
      role='presentation'
      width='100%'
      cellpadding='0'
      cellspacing='0'
      style='background-color: #f4f6f8; padding: 24px 12px;'
    >
      <tr>
        <td align='center'>
          <table
            role='presentation'
            class='container'
            cellpadding='0'
            cellspacing='0'
          >
            <tr>
              <td style='padding: 12px 0; text-align: center;'>
                {{#if logoUrl}}
                  <img
                    src='{{logoUrl}}'
                    alt='{{appName}}'
                    width='120'
                    style='display:block; margin: 0 auto;'
                  />
                {{else}}
                  <strong
                    style='font-size:18px; color:#0f172a'
                  >{{appName}}</strong>
                {{/if}}
              </td>
            </tr>

            <tr>
              <td class='card'>
                <h1>Hello{{#if name}}, {{name}}{{/if}} 👋</h1>
                <p>Click the button below to confirm your email for
                  <strong>{{appName}}</strong>. The code is valid for
                  <strong>{{expirationMinutes}}</strong>
                  minutes.</p>

                {{#if verifyUrl}}
                  <p style='text-align:center; margin-bottom: 20px;'>
                    <a href='{{verifyUrl}}' class='btn'>Verify my email</a>
                  </p>
                {{/if}}
                <hr
                  style='border:none; border-top:1px solid #e6eef6; margin: 20px 0;'
                />

                <p class='muted'>If you didn't request this, you can ignore this
                  email or contact our support at
                  <a
                    href='mailto:{{supportEmail}}'
                    style='color:#0b74ff'
                  >{{supportEmail}}</a>.</p>
              </td>
            </tr>

            <tr>
              <td class='footer'>
                <div>Thanks — the {{appName}} team</div>
                <div style='margin-top:8px; color:#9aa8bd; font-size:12px;'>This
                  verification code will expire in
                  {{expirationMinutes}}
                  minutes.</div>
              </td>
            </tr>

          </table>
        </td>
      </tr>
    </table>
  </body>
</html>