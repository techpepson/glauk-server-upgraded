<html lang='en'>
  <head>
    <meta charset='utf-8' />
    <meta name='viewport' content='width=device-width, initial-scale=1' />
    <title>{{appName}} — Verification successful</title>
    <style>
      :root {
        color-scheme: light;
      }
      body {
        margin: 0;
        background: #f6f8fa;
        font-family:
          -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto,
          'Helvetica Neue', Arial;
        color: #0f172a;
      }
      .wrap {
        max-width: 720px;
        margin: 36px auto;
        padding: 24px;
      }
      .card {
        background: #fff;
        border-radius: 10px;
        padding: 32px;
        box-shadow: 0 6px 20px rgba(15, 23, 42, 0.06);
        text-align: center;
      }
      .logo {
        max-width: 140px;
        margin: 0 auto 18px;
      }
      .icon {
        font-size: 48px;
        line-height: 1;
        margin-bottom: 10px;
      }
      h1 {
        margin: 6px 0 8px;
        font-size: 22px;
      }
      p {
        margin: 0 0 16px;
        color: #475569;
        font-size: 15px;
      }
      .btn {
        display: inline-block;
        background: #0b74ff;
        color: #fff;
        text-decoration: none;
        padding: 12px 18px;
        border-radius: 8px;
        font-weight: 600;
      }
      .secondary {
        display: inline-block;
        margin-left: 10px;
        background: transparent;
        border: 1px solid #e6eef6;
        color: #0f172a;
        padding: 10px 14px;
        border-radius: 8px;
        text-decoration: none;
      }
      .note {
        margin-top: 18px;
        color: #94a3b8;
        font-size: 13px;
      }
      @media (max-width: 480px) {
        .card {
          padding: 20px;
        }
        h1 {
          font-size: 20px;
        }
      }
    </style>
  </head>
  <body>
    <div class='wrap'>
      <div class='card' role='main' aria-labelledby='verified-title'>
        {{#if logoUrl}}
          <a class='logo' href='{{logoUrl}}' aria-label='{{appName}} logo'>
          </a>
        {{/if}}

        <div class='icon' aria-hidden='true'>✅</div>

        <h1 id='verified-title'>Email verified{{#if name}}, {{name}}{{/if}}
          🎉</h1>

        <p>Thanks — we've successfully verified your email address for
          <strong>{{appName}}</strong>. You can now continue to the app.</p>

        <p>
          {{#if loginUrl}}
            <a class='btn' href='{{loginUrl}}'>Go to sign in</a>
          {{else if homeUrl}}
            <a class='btn' href='{{homeUrl}}'>Continue to {{appName}}</a>
          {{else}}
            <a class='btn' href='#'>Continue</a>
          {{/if}}
        </p>

        <p class='note'>If you need assistance, contact
          <a href='mailto:{{supportEmail}}'>{{supportEmail}}</a>.</p>
      </div>
    </div>
  </body>
</html>